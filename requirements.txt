pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
wordcloud>=1.9
Sastrawi>=1.0.1

streamlit>=1.32
altair>=5.0

joblib>=1.3
jupyter>=1.0

# torch/transformers/datasets/accelerate are intentionally NOT listed here.
# Section 5 of the notebook (IndoBERT fine-tune) installs them itself in its
# Colab bootstrap cell, since that section only runs on Colab GPU anyway.
# Keeping them out of this file keeps local installs and the Streamlit Cloud
# deploy (which reads this file) fast and lightweight.
